package com.capstonebe.capstonebe.returnrecord.repository;

import com.capstonebe.capstonebe.returnrecord.entity.ReturnRecord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReturnRecordRepository extends JpaRepository<ReturnRecord, Long> {
}
